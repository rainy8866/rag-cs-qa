# 复制本文件为同目录下的 .env 并填入真实密钥
# DeepSeek API Key（必填，问答与评估集自动生成都依赖它）
DEEPSEEK_API_KEY=

# 大模型型号。已固定为 deepseek-chat（官方稳定的通用对话型号，生成/改写均用它）。
# deepseek-v4-flash 仅用于特定场景人工排查（如坏例根因分析）时手动切换，不建议设为默认。
LLM_MODEL=deepseek-chat

# 国内网络下载 bge-m3 用镜像（可选）
# HF_ENDPOINT=https://hf-mirror.com