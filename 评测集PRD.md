# 评测集PRD：文档源扩容 + 评测集扩容 + 语义去重

> 版本：v1.0 · 更新日期：2026-08-29
> 定位：给 code 模式的可执行开发规格。本 PRD 描述三个需求：**① 文档源扩容 ② 评测集扩容（含难例/边界例/标签） ③ 评测集语义去重**。
> 前置依赖：总 PRD（`PRD_项目总文档.md`）§6.0/§6.5/§8.3 的既有评估体系与版本快照机制。
> 配套阅读：`PRD_项目总文档.md`（§6 评测体系 / §8.4 坏例溯源 / §9.3 评测体系升级）、`01_mvp/core/gold_set.py`、`01_mvp/core/gold_set_enhance.py`。

---

## 0. 背景与目标

### 0.1 背景
当前评估体系存在两个可信度问题（总 PRD §8.4 结论）：
1. **样本量小、文档量小 → 指标虚高**：文档源仅 52 块、评测集仅 54 条，召回率/引用准确率 98.15% 掩盖真实短板（如 rerank 无效、阈值过拟合），说服力不足。
2. **去重停留在文本级**：仅按 question 精确文本去重，措辞近似的问题会残留（如两条「会员折扣与优惠券叠加」不同表述），易导致同知识点重复计分、指标失真。

### 0.2 目标
1. **文档源扩容**：块数 52 → **约 300**（目标区间 200~500），补充 8 份同平台客服 FAQ 文档，暴露跨主题检索问题。
2. **评测集扩容**：条数 54 → **100~150**，加入难例/边界例，并为每条打**场景标签 + 难度标记**。
3. **语义去重**：从「question 文本精确去重」升级为「question 语义级去重」（bge-m3 余弦相似度 ≥ 阈值判重）。
4. **重跑三项指标**并新建版本快照，与旧基线对比；**预期指标回落，如实记录**（样本大、题变难是正常现象，回落即暴露真实问题）。
5. 所有产物落在桌面项目 `C:\Users\qzh-0\Desktop\rag-cs-qa\` 下。

---

## 1. 改动边界

### 1.1 允许修改 / 新增
| 文件 | 动作 | 说明 |
|---|---|---|
| `04_文档源/*.md` | **新增** | 6~10 份客服场景 FAQ 文档（见 §3） |
| `01_mvp/core/config.py` | **改** | 新增 `SEMANTIC_DEDUP_THRESHOLD`、`GOLD_TARGET`、`HARD_CASE_TARGET`（见 §5.1） |
| `01_mvp/core/gold_set_enhance.py` | **改** | 新增难例/边界例生成、tag 字段、语义去重（见 §4/§5） |
| `01_mvp/core/gold_set.py` | **微调** | 新增 `generate_gold_set(num)` 的 num 扩大调用入口（如需要），不改既有行为 |
| `PRD_项目总文档.md` | **改** | §6.5 新增"评测集扩容后"行；§8.3 更新块数/条数；§8.4 按需更新（见 §6） |
| `03_测评集/reports/metrics_<时间戳>.md` | **新增** | 扩容后指标报告 |
| `03_测评集/reports/goldset_audit_<时间戳>.md` | **新增** | 扩容后抽检报告（含语义去重明细 + tag 分布） |

### 1.2 禁止改动
- `core/retrieval.py`、`core/qa.py`、`core/metrics.py`、`core/badcase.py`、`core/llm.py`、`core/embeddings.py`、`core/vectorstore.py`、`core/ingest.py`、`ui/*` —— **全部不动**。
- **禁止**修改 `gold_set_enhance.py` 中既有的 `enhance()` 返回结构 / 抽检逻辑语义；只允许扩展（新增参数与函数）。
- 旧快照（`03_测评集/snapshots/`）**不得覆盖、不得删除**。

---

## 2. 总流程

```
① 接入新文档(04_文档源/*.md)
      ↓
② 重建知识库(ingest.build_index) + 记录新块数(目标 200~500)
      ↓
③ 生成/扩容评测集：自动基线(大 num) + 口语化补充 + 难例/边界例补充
      ↓
④ 语义去重(bge-m3 两两余弦 ≥ 阈值) + 文本级去重(保留)
      ↓
⑤ 抽检(snippet 逐字校验 / 答案完整性 / 问法 5 类 / tag 分布)
      ↓
⑥ 合并写回 gold_set.json(新版本) + 保存文档快照(新建版本)
      ↓
⑦ 重跑三项指标 → metrics_<时间戳>.md → 与旧基线对比
      ↓
⑧ 更新总 PRD §6.5 / §8.3 / §8.4
```

---

## 3. 文档源扩容

### 3.1 目标与来源
- **目标**：新文档 + 旧文档合计入库块数 **200~500**（目标 ~300）。
- **来源要求**：公开、无版权争议、客服问答场景相关（开源项目官方 FAQ 优先）。
- **主题清单（已完成，2026-08-29）**：已人工撰写 **12 份同平台客服 FAQ**（每份约 1000~2200 字，合计约 2 万字），与既有 `customer_service_faq.md` 同一虚构平台、术语一致，覆盖：促销价保/积分会员/订单修改/特殊品类售后/跨境海外/运费配送/支付风控/会员日专属/商品咨询/账户安全/服务承诺/企业采购。文档内已埋入难例素材（价保时效、定金退款、积分到期、生鲜赔付门槛、满减边界等）。
- **格式**：`.md`，保留 `#/##` 标题层级（`ingest.py` 标题感知切块依赖）；如需验证多格式链路，可另用 1~2 份 docx/pdf 经 markitdown 转码后入库（走既有「🔄 转码文档源」）。

### 3.2 重建与快照
1. `ingest.build_index()` 重建向量库（清空重建，chunk_id 重排）。
2. 记录 `n_chunks`，与旧值（52）对比写入报告。
3. `gold_set._save_snapshot(version)` 新建版本快照（`vYYYYMMDD_HHMMSS`），**不得覆盖**旧快照。
4. `gold_set._doc_fingerprint()` 必然变化（文档增删），确认 `gold_set.json` 新版本的 `doc_set` 为新指纹。

### 3.3 验收
- [ ] `n_chunks ∈ [200, 500]`（建议 ~300）。
- [ ] 新文档的 5 个以上知识点能被对应问题检索命中（抽查）。
- [ ] 新版本快照已保存，`doc_set` 指纹已更新。

---

## 4. 评测集扩容

### 4.1 条数构成（目标 100~150 · 增量模式）
| 组成 | 条数 | 说明 |
|---|---|---|
| 保留现有评估集 | 54 | 直接复用（含手工口语化 11 条）；旧文档保留，故旧 snippet 仍逐字命中 |
| 自动基线补充（按问法类型补齐） | ~30 | 统计现有 54 条问法分布，对覆盖不足的类型定向补足 |
| 口语化补充 | ~10 | 复用 `_generate_colloquial`，补齐客服真实口语化场景 |
| 难例/边界例（新增） | ~25 | `_generate_hard_cases`（见 §4.2） |
| 合计 | ~115~120 | 语义去重后最终落盘 |

> **采用增量模式（已定）**：保留现有 54 条，只新增"按类型未覆盖/难例"部分，避免重复生成旧题、省 token；新条目与旧集合并后统一做语义去重（见 §5）。`version`/`doc_set`/快照随新版本更新，旧快照不覆盖。

### 4.2 难例/边界例（新增 `_generate_hard_cases`）
在 `gold_set_enhance.py` 新增 `HARD_CASE_PROMPT` 与 `_generate_hard_cases(n, all_text, existing_q)`：
- **难例类型**（每种若干条，Prompt 中显式要求覆盖）：
  1. 泛查询（如「你们的退货政策是啥」）
  2. 易混淆（两个相近政策放一起，如「7 天无理由」vs「质量问题换货」）
  3. 假设性（如「如果我超过 7 天才申请，还能退吗」）
  4. 否定/排除式（如「哪种情况不能退款」）
  5. 长尾实体（SKU / 政策编号 / 专有名词）
  6. 跨章节组合（答案分散在两个标题下）
  7. 边界条件（如「满减门槛刚好等于 / 差 1 元」）
- **生成要求**（Prompt 约束）：
  - 每条含 `question / expected_answer / required_snippet / tag`；
  - `required_snippet` 必须**逐字复制原文**（沿用 `_snippet_valid` 校验，无效直接过滤）；
  - `expected_answer` 若原文不足以完整回答，写明"根据给定信息无法完全确认"；
  - `tag` 从标签池选 1 个（见 §4.3）。
- **过滤**：与 `existing_q` 文本重复的丢弃；后续再经语义去重收敛。

### 4.3 标签体系（tag）+ 难度标记（difficulty）
- 每条 item 增加两个字段：
  ```json
  {"question": "...", "expected_answer": "...", "required_snippet": "...",
   "tag": "退款", "difficulty": "hard"}
  ```
- **tag 标签池**（预置，Prompt 中给出）：`退货/退款/物流/发货/支付/发票/会员/优惠券/账号/安全/售后/换货/跨境/其他`。
- **difficulty 取值**：`normal`（自动基线/口语化）/ `hard`（难例）/ `boundary`（边界例）。
- **旧条目兼容**：对既有 54 条（无 tag 字段）做一次回填——按 question 关键词规则自动打 tag（命中标签池关键词即回填），difficulty 默认 `normal`；回填规则写入抽检报告。

### 4.4 扩容实现改动（`gold_set_enhance.py`）
- `enhance()` 签名扩展为 `enhance(n_auto=30, n_colloquial=10, n_hard=25, do_semantic_dedup=True)`（增量模式：保留现有评估集，只新增）。
- 新增 `_generate_by_type(n, items, all_text)`：统计现有条目的问法分布（复用 `_classify_question`），对覆盖不足的问法类型定向生成补足。
- 流程插入：① 保留现有 54 条 + 统计问法分布 → ② 按问法类型补齐自动问题（`_generate_by_type`）→ ③ 口语化补充 → ④ 难例补充 → ⑤ 文本级去重 → ⑥ 语义去重（见 §5）→ ⑦ tag 回填 → ⑧ 合并写回 + 快照 → ⑨ 抽检报告。
- 抽检报告新增两节：**tag 分布表**（按 tag 统计条数）、**语义去重明细**（见 §5.3）。

### 4.5 验收
- [ ] 总条数 `∈ [100, 150]`（语义去重后）。
- [ ] 每条含 `tag` + `difficulty` 字段；tag 分布表已产出。
- [ ] snippet 无效条数 = 0（dropped = 0 或已在报告如实记录）。
- [ ] 问法 5 类均有覆盖（报告展示）。
- [ ] 新版本快照已保存，旧快照未覆盖。

---

## 5. 语义去重

### 5.1 新增配置常量（`config.py`）
```python
SEMANTIC_DEDUP_THRESHOLD = 0.90   # question 两两余弦相似度 ≥ 此值视为语义重复
GOLD_TARGET = 115                  # 评测集扩容目标条数（参考值）
HARD_CASE_TARGET = 25             # 难例/边界例目标条数（参考值）
```

### 5.2 实现（`gold_set_enhance.py` 新增 `_semantic_dedup`）
- 输入：`items`（含 tag/difficulty 字段）、`threshold`。
- 步骤：
  1. 用 `core.embeddings.embed_query` 对每条 `question` 向量化（复用 bge-m3，normalize 后点积即余弦）；
  2. 两两计算余弦相似度，构建"重复对"列表；
  3. 对每个重复对，保留**信息量更大**的一条（优先级：`expected_answer` 更长 → `difficulty` 为 hard/boundary 优先保留 → 靠前保留），另一条移除；
  4. 贪婪消解（按相似度从高到低处理，移除后的条目不参与后续配对）。
- 返回 `(deduped_items, removed_pairs)`，`removed_pairs` 为 `[{q1, q2, sim, kept_index, removed_index}]`。

### 5.3 报告
- 在抽检报告中新增「语义去重明细」：每行 `q1 / q2 / 相似度 / 保留哪条 / 移除哪条`。
- 记录去重前条数、去重后条数、移除对数。

### 5.4 阈值标定
- **初始 0.90 的依据（经验先验，非拍脑袋，也非终值）**：
  - bge-m3 对"完全相同问题"的余弦 ≈ 1.0；对"同义改写/措辞近似"通常在 **0.85~0.95**；对"不同知识点但主题相近"一般 **< 0.80**；
  - 0.90 卡在"同义改写带"的高端：能收敛明显近似重复，又尽量不误伤真正不同的问题；
  - 该值会随 embedding 模型/语言/问法长短漂移，**必须实测校准**。
- 跑完做**快速标定**：抽样 20~30 对（10 对已知重复 + 10 对已知不同知识点）看相似度分布，确认 0.90 落在两团之间；再人工抽查被删样本：
  - 若误删较多（两条其实不同知识点）→ 调高至 `0.92 / 0.95` 重跑；
  - 若漏删仍明显（明显同义未被收敛）→ 调低至 `0.85` 重跑。
- 阈值与抽查结论写入报告，不悄悄调参。

### 5.5 验收
- [ ] 语义去重执行成功，`removed_pairs` 明细已记录。
- [ ] 手工抽查 10 条被删样本，判断合理（无把不同知识点误删）。
- [ ] 阈值与抽查结论写入报告。

---

## 6. 指标重跑与对比

### 6.1 重跑三项指标（复用现有函数，不新增逻辑）
- 召回率：`metrics.recall_at_k(gold_items)`（`TOP_K=3`）
- 引用准确率：`metrics.citation_accuracy(gold_items)`
- 坏例：`metrics.run_all(gold_items)` 中的 `badcase`
- **注意**：文档与切块变化后 chunk_id 全重排，`metrics._valid_items` 会重新做 snippet→chunk 映射；`dropped` 数量变化需在报告中如实记录。

### 6.2 对比要求
1. 生成 `03_测评集/reports/metrics_<时间戳>.md`：新指标 + 与旧基线差值 + `n_chunks` 变化 + `dropped` 变化 + 坏例明细。
2. **预期指标回落**（样本大、题变难）。不得修改历史数据；回落即为暴露真实短板，记录在案，作为下一步"坏例驱动优化"的输入。
3. 更新 `PRD_项目总文档.md`：
   - §6.5 基线表：新增一行「评测集扩容后（2026-08-29，N 条）」，填入新指标；
   - §8.3：更新块数（200~500）/ 条数（100~150）；
   - §8.4：若旧坏例消失/新增坏例，追加「评测集扩容后坏例溯源」小节。
4. 保留全部历史记录，**不删除**任何基线行（含双口径记录）。

---

## 7. 目录与路径约定

项目根目录即 `C:\Users\qzh-0\Desktop\rag-cs-qa\`，所有读写路径基于 `core/config.py` 常量（`DOC_DIR`/`GOLD_DIR`/`REPORT_DIR`/`SNAPSHOT_DIR`），**禁止写死绝对路径**。

开发完成后确认：
- `04_文档源/` 新增 6~10 份 FAQ md；
- `01_mvp/core/config.py`、`01_mvp/core/gold_set_enhance.py`（及必要的 `gold_set.py`）已修改；
- 向量库已重建，新版本快照已保存（旧快照未覆盖）；
- `03_测评集/gold_set.json` 为扩容后新版本（100~150 条，含 tag/difficulty，doc_set 为最新指纹）；
- `03_测评集/reports/metrics_<时间戳>.md` 与 `goldset_audit_<时间戳>.md` 已生成；
- `PRD_项目总文档.md` §6.5/§8.3/§8.4 已更新。

---

## 8. 验收清单

- [ ] `04_文档源/` 新增 6~10 份客服 FAQ md（含标题层级）。
- [ ] `config.py` 新增 `SEMANTIC_DEDUP_THRESHOLD / GOLD_TARGET / HARD_CASE_TARGET`。
- [ ] 重建知识库成功，`n_chunks ∈ [200, 500]`（建议 ~300），已记录并与 52 对比。
- [ ] 新版本快照已保存，旧快照未覆盖，`doc_set` 指纹为最新。
- [ ] 评测集总条数 `∈ [100, 150]`（语义去重后），每条含 `tag` + `difficulty`。
- [ ] 难例/边界例 ~25 条覆盖 §4.2 的 7 类难例类型。
- [ ] 语义去重执行成功，`removed_pairs` 明细 + 阈值抽查结论写入抽检报告。
- [ ] snippet 无效条数 = 0（或如实记录），问法 5 类全覆盖。
- [ ] 三项指标重跑完成，`metrics_<时间戳>.md` 已生成（含新旧对比、块数/条数/dropped 变化、坏例明细）。
- [ ] `PRD_项目总文档.md` §6.5/§8.3/§8.4 已更新，历史记录未删除。
- [ ] 所有产物均在桌面项目文件夹下。

---

## 9. 待确认项与风险

### 9.1 待确认项
1. 语义去重阈值 `0.90` 是否合适？需人工抽查后定，误删调高、漏删调低。`[Assumption — to be confirmed]`
2. 文档源扩容到 ~300 块（**已确认够用**，2026-08-29）；若重跑后块数不足 200 或仍显单薄，可再加主题文档。`[Decision — 已确认 ~300]`
3. ~~自动基线用"大 num 全量重生成 + 语义去重"还是"旧集保留 + 增量补充"？~~ **已定：增量模式**（保留现有 54 条，按问法类型/难例类型定向新增，省 token 且保住手工口语化问题）。`[Decision — 2026-08-29 已改增量]`

### 9.2 风险
| 风险 | 缓解 |
|---|---|
| 文档扩容后跨主题干扰、指标回落幅度大 | 回落如实记录，作为坏例驱动优化的输入；不掩盖 |
| 难例生成 snippet 无效率高 | `_snippet_valid` 过滤 + LLM 修复兜底；无效条目丢弃并记录 |
| 语义去重误删不同知识点 | 阈值可调 + 人工抽查 10 条 + 报告留痕 |
| 重生成导致旧口语化问题丢失 | 若抽查发现丢失严重，切换"增量补充"方案（§9.1-3） |
| 生成/去重 API 调用量大、耗时长 | 分批（沿用 BATCH=5 机制）；语义去重为本地 bge-m3，无 API 成本 |
