# RAG 客服问答平台 — 项目 PRD（合并版）

> 版本：v1.1（合并版）· 更新日期：2026-08-29
> 来源：由《实现规格.md》+《PRD_文档源多格式转换接入.md》(v2.0) +《二期优化路线.md》三文档合并去重而成，统一到项目最新状态；v1.1 并入《新需求2prd.md》（切块粒度优化方案 A + 指标重跑对比）与《新需求prd.md》（评估集增强）内容。
> 用途：作为项目总 PRD —— 开发依据 + 已实现现状 + 未来优化规划。
> 结构速览：§0-§2 总览与需求 → §3-§6 系统实现契约 → §7 运行顺序 → §8 实现与自测记录（现状） → §9 未来优化方向 → §10 作品集沉淀。

---

## 0. 项目总览

- 项目名：`rag-cs-qa`
- 定位：个人练手项目，但要求"成熟、可落地、MVP 后能迭代、别人也能用"。目标岗位方向：AI 产品经理（重点沉淀量化评估与数据驱动迭代方法论）。
- 本次范围（MVP）：本地网页问答 + 答案带引用出处 + 一份可重复跑的评测报告 + 多格式文档源一键转码接入。
- 大模型：**DeepSeek**（走 API）。默认模型名 `deepseek-v4-flash`，**可在 `.env` 用 `LLM_MODEL` 覆盖**（若平台未提供该型号，改回 `deepseek-chat`）；启动/调用时**须自检模型名**并在不存在时报清晰错误，不得静默失败。Embedding 用本地 **bge-m3**（免费、中英都好）。
- 技术栈：Python 3.11 + **Streamlit(前端，带聊天 + 评测/优化按钮)** + ChromaDB(向量库) + sentence-transformers/FlagEmbedding(bge-m3) + OpenAI 客户端(调 DeepSeek) + **markitdown(多格式文档转码)** + 脚本评测。
- 文档源（示例知识库）：LangChain 官方文档（脚本下载）+ 离线兜底样例 + **客服 FAQ 多格式测试文档（docx）**。
- 项目位置：**桌面 `Desktop\rag-cs-qa\`**，内分子文件夹：`01_mvp`(应用代码)、`02_优化`、`03_测评集`、`04_文档源`、`05_运行与交付`。

---

## 1. 需求与验收

### 1.1 这个项目要解决的问题
把散落的公开/企业文档（Word/PDF/Excel/PPT/Markdown），变成一个"输入问题 → 得到带引用来源的稳定答案"的知识问答，并**用量化指标证明自己答得准**。

### 1.2 MVP 验收线（已全部达成，见 §8）
1. 网页能提问，回答中带引用（`[1][2]…`）且前端能折叠展示出处。
2. 能上传/接入示例文档建知识库（至少走通"读文档→切块→向量化→入库"脚本）。
3. 支持 **docx/pdf/xlsx/pptx → md 一键转码**后入库，兼容真实客服文档格式。
4. 内置评测：跑通 40+ 条评估集，输出**召回率 + 引用准确率 + 坏例清单**三样东西。

### 1.3 二期（未来优化，详见 §9）
Rerank ✅(2026-08-29)、混合检索、查询改写 ✅(2026-08-29)、引用后校验 ✅(2026-08-29)、切块粒度优化、坏例驱动的自动优化 + 改前/改后对比图、产品化落地。

---

## 2. 技术栈与依赖

`01_mvp/requirements.txt`：
```
streamlit
chromadb
sentence-transformers
FlagEmbedding
openai
httpx
beautifulsoup4
markdownify
python-dotenv
markitdown[docx,pdf,xlsx,pptx]   # 多格式文档转码（已装 0.1.7）
```

模型与关键配置：
- LLM：DeepSeek。默认 `LLM_MODEL="deepseek-v4-flash"`，可由 `.env` 覆盖（不存在时报错并提示改用 `deepseek-chat`）；OpenAI 兼容接口：`base_url = https://api.deepseek.com`。
- Embedding：本地模型 `BAAI/bge-m3`（向量维度 1024），首次运行会从 HuggingFace 下载（国内需设 `HF_ENDPOINT=https://hf-mirror.com`）。
- Rerank（✅ 已实施 2026-08-29，见 §9.1.4）：`BAAI/bge-reranker-v2-m3`，`core/rerank.py` 用 **transformers 直调**（AutoModel+AutoTokenizer 交叉编码器），不依赖 FlagEmbedding（其旧 API `prepare_for_model` 在 transformers≥5 崩溃）。
- 向量库：ChromaDB，统一持久化到 **`01_mvp/.vector_store/`**。
- 密钥：放进 `.env`（`DEEPSEEK_API_KEY=xxx`），代码用 `python-dotenv` 读取。

---

## 3. 目录结构（当前最新）

```
Desktop\rag-cs-qa\
├── PRD_项目总文档.md             # ★ 本项目总 PRD
├── 实现规格.md / PRD_文档源多格式转换接入.md / 二期优化路线.md   # 合并前来源（历史保留）
├── 简历项目描述.txt              # 简历材料
├── 01_mvp\                      # ★ 主应用(Streamlit)
│   ├── app.py                   # 入口：侧边栏导航「智能问答 / 评测与优化」
│   ├── core\
│   │   ├── config.py            # 读取模型/向量库常量 + 各子文件夹路径 + DOC_CONVERT_ENGINE
│   │   ├── ingest.py            # 读文档 → 切块 → embedding → 写 Chroma
│   │   ├── convert_docs.py      # ★ 多格式转码：docx/pdf/xlsx/pptx → md（markitdown/anydoc）
│   │   ├── retrieval.py         # 检索 top-k（返回 chunk 及来源）
│   │   ├── qa.py                # 检索 + 拼 prompt + 生成带引用答案
│   │   ├── gold_set.py          # ★ 评估集自动生成(无需手写)
│   │   ├── metrics.py           # 召回率/引用准确率计算
│   │   └── badcase.py           # 坏例归类
│   ├── ui\
│   │   ├── chat.py              # 「智能问答」页(聊天+引用折叠)
│   │   ├── eval.py              # 「评测与优化」页(转码按钮+按钮+指标仪表盘+对比图)
│   │   └── style.py             # 页面美化
│   ├── requirements.txt
│   ├── .env.example
│   └── README.md
├── 02_优化\                     # 二次优化
│   ├── rerank.py                # bge-reranker 重排(提召回)
│   ├── optimize.py              # 一键优化：针对坏例调参/切换检索策略
│   ├── optimize_log.md          # 坏例→优化→改动记录
│   └── compare\                 # 优化前后指标对比图/报告
├── 03_测评集\                   # 评估集 & 报告
│   ├── gold_set.json            # 评估集(54 条，v20260826_015159)
│   ├── gold_set_manual.json     # (可选)手工覆盖/补充
│   ├── snapshots\               # 版本快照（文档源 + 配置）
│   └── reports\                 # 指标报告
├── 04_文档源\                   # 知识库原始文档
│   ├── fetch_docs.py            # 下载 LangChain 文档
│   ├── offline_01.md …          # 离线兜底样例
│   └── customer_service_faq.docx # ★ 客服 FAQ 测试文档（3000+ 字，转码后入库）
└── 05_运行与交付\               # 运行说明 + 作品集材料
    ├── 一键运行.bat
    └── 演示录屏说明.md
```

> 路径约定：代码统一从 `01_mvp` 运行；文档源读 `../04_文档源`、评估集读写 `../03_测评集`、优化产物写 `../02_优化`，统一在 `config.py` 用相对项目的常量定义。

---

## 4. 文档源与多格式转换

### 4.1 文档源获取
1. `04_文档源/fetch_docs.py`（推荐）：GET `https://python.langchain.com/sitemap.xml` → 解析含 `/docs/` 的 URL 取前 30 条 → 逐条 HTML 转 Markdown 存 `doc_<序号>.md`；单页失败跳过不影响整体。
2. 离线兜底：`04_文档源/offline_*.md` 至少 3 份手写 Q&A 样例，每份 5–8 条，内容真实、有可命中知识点。
3. 多格式测试文档：`04_文档源/customer_service_faq.docx`（客服常见问题 FAQ，3000+ 字、9 标题、2 表格、3 个超长段落），用于验证转码链路。

> 请勿整站爬取无授权站点。

### 4.2 多格式转码需求（docx/pdf/xlsx/pptx → md）
- **目标**：真实客服/企业文档源多为 Word/PDF/Excel/PPT，需一键转成 `ingest.py` 能读的 Markdown，复用现有切块入库逻辑，**不改动 ingest 的切块与入库行为**。
- **方案**：`core/convert_docs.py` 扫描 `04_文档源`，对支持后缀生成同名 `.md`；引擎由 `config.DOC_CONVERT_ENGINE` 切换（`markitdown` 默认 / `anydoc` 可选）。
- **关键契约**：转换产物必须保留 `#/##` 标题层级（`ingest.py` 的标题感知切块依赖它）；若引擎输出无标题（如 PDF/表格），由 `convert_docs._ensure_heading` 兜底补 `# <文件名>`。
- **容错**：同名 `.md` 跳过不覆盖；单文件异常进 `errors` 不中断整批；不支持后缀忽略；未知引擎返回错误不崩溃。
- **UI 入口**：评测页「🔄 转码文档源」按钮，展示转换/跳过/失败计数；转码后需「🗂️ 重建知识库」入库。

### 4.3 已实现与自测结论（详见 §8）
- 4 格式转码、跳过同名、异常隔离、ingest 复用、引擎降级 6 项自测全部通过。

---

## 5. 系统模块实现要求（关键契约）

### 5.1 `01_mvp/core/config.py`
- 从 `.env` 读 `DEEPSEEK_API_KEY`、`LLM_MODEL`（默认 `deepseek-v4-flash`，可覆盖为 `deepseek-chat`）、可选 `HF_ENDPOINT`。
- 常量：`EMBED_MODEL="BAAI/bge-m3"`、`CHUNK_SIZE=500`、`CHUNK_OVERLAP=50`、`TOP_K=5`、`RERANK_CANDIDATES=20`、`COLLECTION_NAME="docs"`、`SIMILARITY_METRIC="cosine"`、`MIN_SCORE=0.35`（**需用评估集标定**，见 §6）、`DOC_CONVERT_ENGINE="markitdown"`。
- `verify_llm() -> bool`：启动自检模型名，不存在则报错并提示改 `deepseek-chat`，不得静默失败。
- 路径常量（基于项目根，用相对 `01_mvp` 计算）：`PROJECT_ROOT`、`DOC_DIR`、`VECTOR_DIR`、`GOLD_DIR`、`REPORT_DIR`、`OPT_DIR`、`COMPARE_DIR`。

### 5.2 `01_mvp/core/ingest.py`
- 遍历 `DOC_DIR/*.md`。
- **切块策略（结构感知，禁止固定字符硬切）**：
  1. 按 Markdown 结构逐级下钻：`## / # 标题 → 段落(空行分隔) → 句子`，优先每块对应一个完整章节/段落；
  2. 只有超过 `CHUNK_SIZE` 的长段落才按**句子边界**切分，并保留 `CHUNK_OVERLAP` 重叠，避免从句子中间截断；
  3. **细粒度切分（CHUNK_MIN_SIZE=150，方案 A）**：段落长度超过 `CHUNK_MIN_SIZE` 且含多个句子时，即使未超过 `CHUNK_SIZE`，也按句子边界切分成更聚焦的块（`flush_para` 判定），解决"混合大块稀释查询语义"问题（见 §8.4）；短段落（≤150 字或单句）仍整段成块保留上下文；
  4. 目标：**「一个块 = 一个完整知识点」**，每块约 300–500 字。优先级：语义完整 > 字数达标。
- 每块元数据：`{source, chunk_id, heading}`（heading 用于引用时展示"来自哪一节"）。`chunk_id` 为切块自动序号，仅供内部定位。
- 用 bge-m3 向量化写入 Chroma（持久化到 `VECTOR_DIR`）；可重复执行（先 delete 再重建）；UI 提供「重新建档」入口。
- `required_chunk_id` 不需手工查填，运行时由 "原文片段"(snippet) 自动映射（见 §6.0）。
- 实现提示：可用 `RecursiveCharacterTextSplitter`（分隔符 `["\n\n", "\n", "。", "！", "？", ". ", "! ", "? "]`）或自写同等递归分割器；不要用固定长度方式。

### 5.3 `01_mvp/core/retrieval.py`
- `retrieve(query, top_k=TOP_K, use_rerank=False) -> list[Chunk]`；`Chunk={id, text, source, heading, score}`。
- **召回方式 = 向量相似度**：bge-m3 编码后全库按 **cosine** 取最相似 top-k。
- **★ 距离↔相似度换算（关键，勿错）**：Chroma 返回的是 **distance（cosine 距离，越小越相似，≈ 1 − 相似度）**。代码**必须**做 `score = 1.0 - distance`，后续排序、`MIN_SCORE` 过滤、阈值标定、指标判定都使用换算后的相似度，过滤统一为 **`score >= MIN_SCORE`**。
- 检索类模型给 query 加统一前缀（如"为这个句子生成表示以用于检索相关文章："）。
- `use_rerank=True`：候选放大到 `RERANK_CANDIDATES`(20) → rerank 精排取 top-k。
- 预留升级位：混合检索（向量 + BM25 + RRF，见 §9.1.2）。

### 5.4 `01_mvp/core/qa.py`
- `answer(question, use_rerank=False) -> {answer, citations:[{chunk_id, source, text}], success}`。
- 流程：`retrieve` → 拼 prompt（§5.5）→ 模型输出带 `[n]` 引用的答案 → 解析映射回 chunk → 组成 citations；若 top-k 为空或模型不配合，`success=False` 并兜底话术（"抱歉，知识库中没有找到相关资料，建议改述或联系人工。"）。
- **引用契约**：只准引用提供的编号资料，不准编造来源。

### 5.5 生成答案的 Prompt 模板
```
你是客服问答助手。只依据下面提供的资料回答，严禁编造。
若资料不足，直接回答"资料不足无法确认"。

资料：
[1] (来源: {source}) {text}
[2] ...

问题：{question}

要求：先给结论；用到哪条资料就在对应句末标 [编号]；最后换行列出"参考资料"编号与文件名。
```

### 5.6 「智能问答」页 `ui/chat.py`
- `st.chat_message` 聊天气泡 + `st.chat_input`。
- 回答渲染：正文 Markdown + 引用角标 `[1]`，每条 `st.expander("参考资料 [1] · 文件名")` 折叠原文。
- 侧边栏开关「是否用 Rerank」（默认关）。
- 首用检测无向量库时给「先去建档」引导按钮。

### 5.7 页面美化 `ui/style.py`
- `st.set_page_config(page_title, page_icon, layout="wide")` + 自定义 CSS（柔和主色 #4F6DF5、圆角卡片、气泡配色、按钮 hover、标题层级）。
- 侧边栏分组：导航项「💬 智能问答」「📊 评测与优化」。

### 5.8 「评测与优化」页 `ui/eval.py`（按钮区）
按功能区分为若干卡片，按钮行为：

**A. 文档源区**
- 按钮「🔄 转码文档源」→ 调 `core/convert_docs.py`，展示转换/跳过/失败计数。
- 按钮「🗂️ 重建知识库」→ `ingest.py` 重建向量库（转码后入库用）。

**B. 评估集区**
- 按钮「🔄 生成评估集」→ `core/gold_set.py` 生成 `03_测评集/gold_set.json`，显示条数。

**C. 指标检测区（三个按钮各自独立触发）**
- 「🎯 检测召回率」→ `metrics.py`，`st.metric("Recall@k", …)` + 「未命中明细」表格。
- 「🎯 检测引用准确率」→ `metrics.py`，`st.metric("引用准确率", …)` + 明细表。
- 「🧪 检测坏例」→ `badcase.py`，坏例分类统计柱状图 + 每类 3–5 个代表性坏例。
- 「⚖️ 标定相似度阈值」→ 遍历评估集画"命中 vs 未命中"分数分布直方图，空隙处自动推荐 `MIN_SCORE` 写回配置。
- 全部跑完显示「📄 导出指标报告」→ 写 `03_测评集/reports/metrics_<时间戳>.md`。

**D. 二次优化区**
- 「🚀 一键优化」→ `optimize.py`。**严格限定只调"不动向量库/不动评估集"的参数**（保证同一张卷子）：只搜索 `TOP_K`、`MIN_SCORE`、`use_rerank`；**不得**改切块参数/embedding（改会 rebuild 全库、`chunk_id` 与评估集映射全失效）。
- 自动尝试候选组合、在同一份 `gold_set` 上评估选最优落地；用图表画优化前后对比，结果写 `02_优化/compare/`。
- 手动开关 `st.toggle("启用 Rerank")` 同步到智能问答页。
- **改切块的正确入口**（单独、显式）：走「重建知识库」→ 重新 `ingest` → **新建版本快照**（新 `gold_set`），不走一键优化。

> 单个按钮内用 `st.spinner` + `st.status` 显示进度；长任务结果写入对应子文件夹，避免页面重跑丢失。

---

## 6. 评测体系（项目招牌）

### 6.0 评估集构建方案（自动基线 + 本人抽检 + 本人手工补充口语化）
- 基线样本由 `gold_set.py` 依据知识库**真实存在的原文**生成 40+ 条评估样本，每条 `{question, expected_answer, required_snippet}`（`required_snippet` 必须是**逐字复制**的原文片段）；运行时 `snippet_to_chunk(snippet)` 自动映射成 chunk_id，找不到标 `DROP`（该条剔除并在报告注明）。
- **本人手工抽检**：对每条逐一核对——去重（问题完全相同仅保留 1 条）、`required_snippet` 是否逐字命中原文、`expected_answer` 是否完整（剔除"无法确认"式残缺）、问法多样性统计（直接询问/请求确认/寻求解释/假设性提问/求例子 5 类）。
- **本人手工补充真实口语化问题**：依据客服真实场景手工撰写 11 条口语化问法（如"我买的东西坏了咋办？""这运费谁出啊？""会员能免运费不？"），贴近真实用户打字/说话习惯，并同样保证 snippet 逐字命中原文。
- **评估集版本快照（★ 迭代基石）**：`gold_set.json` 带元数据 `{version, created_at, doc_set, params:{CHUNK_SIZE, EMBED_MODEL, MIN_SCORE, SIMILARITY_METRIC}}`；生成时同步快照文档源到 `03_测评集/snapshots/<version>/docs_snapshot/` 并保存基线；**任何改变向量库的改动（换文档源/切块/embedding）必须新建版本快照，绝不在旧版本上覆盖**，保证"改前 vs 改后"同一套卷子。
- 可选手写补充 `gold_set_manual.json`，运行时与自动集合并。

### 6.1 `metrics.py`：召回率 `Recall@k`
- 对每条 gold 跑 `retrieve(question, top_k=TOP_K)`，`snippet_to_chunk(required_snippet)` 命中结果中 → 命中。
- 输出 `Recall@k = 命中数 / 有效条数` + 未命中明细表。

### 6.2 `metrics.py`：引用准确率
- 对每条 gold 跑 `answer(question)`；规则化判定（2 天版本够用）：引用 chunk 在本次 top-k 结果中 且 与答案句词级重叠（Jaccard ≥ 0.2）→ 正确；其余错误。
- 输出 `CitationAccuracy = 正确引用数 / 总引用数` + 明细。

### 6.3 `badcase.py`：坏例分析
- 归类（一经发现即记一类）：`recall_miss`（没检索到）/ `citation_error`（引用错乱编造）/ `hallucination`（无依据自圆其说）/ `empty_answer`（空答）。
- 输出：分类统计 + 每类 3–5 个代表性**原始问题/答案/引用/判断**（供人工复盘）。

### 6.4 汇总报告
输出 `03_测评集/reports/metrics_<时间戳>.md`：三项指标 + 坏例分类表 + 未命中明细。**这是作品集最有说服力的基线报告。**

### 6.5 评测指标记录（54 条 → 126 条评估集 · 混合知识库）
| 阶段 | 召回率 | 引用准确率 | 坏例 | 备注 |
|---|---|---|---|---|
| 增强后基线（2026-08-26） | 94.44% (51/54) | 94.44% (51/54) | recall_miss ×3 | 43 块 |
| 切块优化后（2026-08-26） | 94.44% (51/54) | 94.44% (51/54) | recall_miss ×3 | 52 块 · CHUNK_MIN_SIZE=150 |
| 映射修正后（2026-08-26） | 98.15% (53/54) | 98.15% (53/54) | recall_miss ×1 | 52 块 · snippet_to_chunk 取内容最相似块 |
| 评测集扩容后（2026-08-29） | 87.20% (109/125) | 85.60% (107/125) | recall_miss ×16 + citation_error ×2 | 255 块 · 126 条 · 含难例/边界例 · dropped 1 |
| 扩容后 + 改写/引用后校验（2026-08-29） | **88.00% (110/125)** | **88.00% (110/125)** | recall_miss ×15 | 双口径：关=87.20%/85.60%/18 → 开=88.00%/88.00%/15（改写多路召回 + 引用语义后校验，`deepseek-chat`） |
| 扩容后 + 改写 + Rerank（2026-08-29） | **92.00% (115/125)** | **90.40% (113/125)** | recall_miss ×10 + citation_error ×2 | 开含 Rerank 精排（bge-reranker-v2-m3）+ 排序修复（直出 rerank 顺序）：关=87.20%/85.60%/18 → 开=92.00%/90.40%/12（改写多路召回 + Rerank + 引用后校验，`deepseek-chat`） |

- **扩容后回落符合预期**：样本 54→126、文档 52→255 块、加入难例/边界例（23 hard + 6 boundary），指标从 98.15% 回落到 87.20%/85.60%，回落即暴露真实短板（泛查询/跨章节复合问题/边界引用），见 §8.4「评测集扩容后坏例溯源」；不得修改历史数据掩盖回落。
- 引用准确率/坏例此前以 Rerank 关为主口径（2026-08-29 已补跑 Rerank 开全量：召回 87.20%→92.00%、引用 85.60%→90.40%、坏例 18→12，见 §8.4/§9.1.4；注：引用/坏例含 ±1 量级 LLM 非确定性波动，召回为确定性指标）。

- 评估集 54 条 = 自动基线 43 条（经本人抽检全 OK）+ 本人手工补充 11 条真实口语化问题（全部命中、dropped=0）。
- 开/关 rerank 完全一致（坏例全为召回层 miss，rerank 救不了；此为 43/52 块小语料时代的结论，语料扩容到 255 块出现大量"高度相关但答案不同"的同章节竞争块后，Rerank 收益显著，见 §9.1.4）；坏例溯源见 §8.4，优化方向见 §9。
- 切块优化后数字持平的原因见 §8.4：检索实际已命中「会员折扣」目标，但指标判定受重叠 + snippet 映射歧义影响，被误判为 miss。
- **双口径记录**：旧口径（snippet_to_chunk 取「第一个包含的块」）为 94.44%，新口径（取「候选里与 snippet 内容最相似的块」）为 98.15%，两值均保留。修正仅影响 2 条重叠歧义条目（「会员折扣」重复问题，块 15→16，sim=0.86），其余 52 条映射不变，无虚高。
- **改写/引用后校验双口径（2026-08-29，126 条）**：关=87.20%/85.60%/18 → 开=88.00%/88.00%/15（召回 +1、引用 +3、坏例 -3，2 条 citation_error 全消除）。收益有限且如实记录：改写只修好部分泛查询召回，仍坏 15 条期望块在任何子查询 top3 都不出现，属 bi-encoder 排序短板（详见 §8.4），Rerank 为二期针对性解法。报告：`02_优化/compare/compare_rewrite_20260829_065149.md`。
- **改写 + Rerank 双口径（2026-08-29，126 条）**：开含 Rerank 精排（bge-reranker-v2-m3）后 关=87.20%/85.60%/18 → 开=**92.00%/90.40%/12**（召回 +6、引用 +6、坏例 -6）。相对上一轮开（改写+校验，无 Rerank）：召回 +5、引用 +3、坏例 -3（recall_miss 15→10；新增 citation_error 经根因分析为"答案正确、引用块是更直接相关块但 ≠ 评估期望块"，见 §8.4）。两轮存档（排序修复前 13 坏例 / 修复后 12 坏例）：`02_优化/compare/compare_rewrite_20260829_103228.md`、`compare_rewrite_20260829_110735.md`（md + json，均含上一轮无 Rerank 存档对照）。

---

## 7. 运行顺序

```bash
# 0) 项目根：C:\Users\qzh-0\Desktop\rag-cs-qa；在 01_mvp 目录执行；先把 .env.example 复制为 .env 填 DEEPSEEK_API_KEY

cd 01_mvp
pip install -r requirements.txt        # 首次会下载 bge-m3（可先 set HF_ENDPOINT=https://hf-mirror.com）
python ../04_文档源/fetch_docs.py       # 拿知识库；失败则用 offline_*.md（可跳过）

streamlit run app.py                   # 启动 http://localhost:8501

# 在「评测与优化」页按顺序：
#   ① 「🔄 转码文档源」把 04_文档源 里的 docx/pdf/xlsx/pptx 转成同名 md（如有）
#   ② 「🗂️ 重建知识库」入库
#   ③ 「🔄 生成评估集」→ ④ 检测召回率/引用准确率/坏例 → ⑤ 标定阈值 → ⑥ 一键优化（可选）

# 纯命令行等价操作：
python -m core.gold_set                 # 生成评估集
python -m core.ingest                   # 建档
python _validate_rewrite.py             # 只读预验证：坏例看改写/引用后校验是否修正 + 校验阈值标定
python _run_metrics.py                  # 双口径全量指标重跑（关 vs 改写+后校验，并发4线程，落盘 02_优化/compare/）
# 产物落在 03_测评集/reports/ 与 02_优化/compare/
```

---

## 8. 实现与自测记录（现状）

### 8.1 已实现
| 动作 | 文件 | 说明 |
|---|---|---|
| 新增 | `01_mvp/core/convert_docs.py` | 多格式 → md 转换封装（含 `_ensure_heading` 兜底补标题） |
| 修改 | `01_mvp/ui/eval.py` | 新增「🔄 转码文档源」按钮与结果展示 |
| 修改 | `01_mvp/core/config.py` | 新增 `DOC_CONVERT_ENGINE` 常量（markitdown 默认 / anydoc 可选） |
| 修改 | `01_mvp/requirements.txt` | 追加 `markitdown[docx,pdf,xlsx,pptx] 0.1.7`（已安装） |
| 不改 | `01_mvp/core/ingest.py` | 切块/入库逻辑保持原样 |
| 修改（2026-08-26 切块优化） | `01_mvp/core/config.py` | 新增 `CHUNK_MIN_SIZE = 150`（细粒度切块阈值） |
| 修改（2026-08-26 切块优化） | `01_mvp/core/ingest.py` | `flush_para` 增加细粒度切分：超过 CHUNK_MIN_SIZE 的多句段落按句子切，并按 CHUNK_MIN_SIZE 聚合为聚焦小块；`_merge_with_overlap` 新增 `target_size` 参数（默认仍 CHUNK_SIZE） |
| 修改（2026-08-26 映射修正） | `01_mvp/core/gold_set.py` | `snippet_to_chunk` 由「第一个包含」改为「候选里与 snippet 内容最相似的块」（消除重叠映射歧义，双口径依据 §6.5） |
| 新增（2026-08-29 查询改写） | `01_mvp/core/query_rewrite.py` | LLM 拆解复杂问题为 1~3 个子查询（`deepseek-chat`，原问题恒在首位，失败降级为单路）；`config.REWRITE_ENABLED/REWRITE_MAX_QUERIES/REWRITE_MODEL` |
| 修改（2026-08-29 查询改写） | `01_mvp/core/retrieval.py` | 新增 `retrieve_multi(queries)`：多查询按 chunk_id 聚合取最高分，再走阈值/去重/top_k |
| 修改（2026-08-29 引用后校验） | `01_mvp/core/qa.py` | `answer()` 支持 `use_rewrite/use_post_check/queries`；新增 `_post_check_citations()`：逐句语义校验引用（无支撑引用移除 / 非最优块改写为最相似块编号），阈值 `config.CITATION_CHECK_THRESHOLD=0.5` |
| 新增（2026-08-29 双口径评测） | `01_mvp/_run_metrics.py` | 并发 4 线程 + 改写缓存 + LLM 重试，一次跑关/开双口径并落盘 `02_优化/compare/`（md + json） |
| 新增（2026-08-29 预验证） | `01_mvp/_validate_rewrite.py` | 只读预验证：18 条坏例看改写/后校验是否修正 + 引用后校验逐句相似度用于阈值标定 |
| 新增（2026-08-29 Rerank） | `01_mvp/core/rerank.py` | bge-reranker-v2-m3 交叉编码器精排（transformers 直调 AutoModel+AutoTokenizer，规避 FlagEmbedding 旧 API `prepare_for_model` 在 transformers≥5 崩溃被 except 吞掉的问题）；失败静默降级原顺序 |
| 修改（2026-08-29 Rerank） | `01_mvp/core/retrieval.py` | `retrieve/retrieve_multi` 支持 `use_rerank`：候选放大到 `config.RERANK_CANDIDATES=20` 再做精排取 top_k |
| 修改（2026-08-29 Rerank） | `01_mvp/core/qa.py` | `answer()` 支持 `use_rerank` 透传检索精排 |
| 新增（2026-08-29 Rerank 下载） | `01_mvp/_download_reranker.py` | 联网下载 bge-reranker-v2-m3（临时关离线 + `HF_HUB_DISABLE_XET=1` 强制 LFS，规避 hf-mirror 不支持 Xet 的 401） |
| 新增（2026-08-29 Rerank 诊断） | `01_mvp/_diag_rerank.py` / `_diag_rerank_recall.py` / `_diag_rerank2.py` | 验证模型加载、对比开/关 rerank 召回集与打分、全量召回率对比（修好/回归逐条） |

### 8.2 自测结果（6 轮全过）
1. **4 格式转码**：docx/pdf/xlsx/pptx 各产同名 `.md`，均含 `#` 标题；docx 产物完整保留 `# 退换货政策` / `## 7天无理由` 层级。
2. **跳过同名**：重跑 `converted=0, skipped_existing=4`，不覆盖。
3. **单文件异常不中断**：损坏 docx 抛 `BadZipFile` → 进 `errors`，`failed=1`，其余照常。
4. **不支持后缀忽略**：`.txt` 不进任何计数。
5. **ingest 无改动复用**：`read_all_docs()` 读到 4 个转码 md 的切块（总 22 块）。
6. **未知引擎降级**：返回错误 dict 不崩。

### 8.3 测试文档与评估集
- 客服 FAQ 测试文档（`customer_service_faq.docx`）：3000+ 字、9 标题、2 表格、3 个 500+ 长段落；入库 43 块（切块优化前；切块优化后 52 块）。
- 评估集（**54 条**，版本 `v20260826_015159`）= 自动基线 43 条 + 本人手工补充 11 条真实口语化问题（覆盖退货/退款/运费/会员/优惠券/物流/账号等客服场景）。
- 本人抽检结论：去重 0 / snippet 无效 0 / 答案缺失 0，问法覆盖 5 类；抽检报告见 `03_测评集/reports/goldset_audit_20260826_015228.md`，新版本快照已保存。

**文档源扩容 + 评测集扩容（2026-08-29，评测集 PRD）**：
- 文档源：新增 12 份同平台客服 FAQ（促销价保/积分会员/订单修改/特殊品类/跨境/运费/支付风控/会员日/商品咨询/账户安全/服务承诺/企业采购），合计 16 份 md，重建知识库 **255 块**（目标 200~500，旧值 52）。
- 评估集：**126 条**（版本 `v20260829_043818`，`doc_set=80da7244`）= 原 54 条保留 + 按问法类型补齐 37 + 口语化 14 + 难例/边界例 34，**语义去重 159→126**（bge-m3 余弦 ≥ 0.90，移除 33 对同知识点近似题，抽查无误删）。
- 每条含 `tag`（12 类标签池回填）+ `difficulty`（normal 97 / hard 23 / boundary 6）；问法 5 类全覆盖（直接询问 27/其它 19/寻求解释 11/请求确认 7/假设 6/求例子 4）；抽检报告见 `goldset_audit_20260829_044039.md`，新版本快照 `snapshots/v20260829_043818/` 已保存。
- 生成类 LLM 统一切到 `deepseek-chat`（非推理模型）：推理模型 `deepseek-v4-flash` 把输出预算耗尽在 reasoning 上导致难例生成 content 为空、批量欠产（实测修复 2026-08-29）。

### 8.4 坏例溯源（3 条，54 条评估集）
- 「会员折扣与优惠券可叠加」×2：目标块进 top12 但未进 top3（排第 7）；根因 = 235 字「会员体系」混合大块稀释查询语义（**切块粒度问题**）。
- 「退货政策是什么」：目标块排第 5，与第 3 名仅差 0.014；根因 = 泛查询 + 同章节多块竞争。
- 新增 11 条口语化问题全部命中，未引入新坏例；dropped = 0。结论：检索层短板指向 §9.1.1 切块粒度 + §9.1.3 查询改写。

**切块优化后坏例溯源（2026-08-26，52 块，CHUNK_MIN_SIZE=150）**：
- 「会员折扣与优惠券可叠加」×2：**检索已实际命中**——目标句"会员折扣与优惠券可以叠加使用…"排检索 **top1**（score 0.68）；但指标仍判 miss。根因 = 重叠机制把同一句放进**相邻两块**（块 15/16），`snippet_to_chunk` 按"第一个包含"映射到**低分块**（排 top8），而检索命中**高分块**（top1），属**指标判定误判（snippet 映射歧义）**，非真实检索失败。
- 「退货政策是什么」：目标块仍排 top5（score 0.56），切块未改变排名——泛查询 + 同章节多块竞争，属**真实检索排名问题**，切块无帮助。
- 结论：方案 A（切块粒度）确实消除了「会员折扣」的语义稀释（检索从第 7 名提升到 top1），但收益被评估指标的 snippet 映射机制掩盖（指标持平 94.44%）；对「退货政策」类泛查询无效，仍指向 §9.1.3 查询改写。若要消除该误判，需改进 `snippet_to_chunk` 映射（如返回最高分匹配块）或评估集去重叠，列为后续优化项。

**映射修正后坏例溯源（2026-08-26，snippet_to_chunk 取内容最相似块）**：
- 「会员折扣」×2：`snippet_to_chunk` 由「第一个包含」改为「候选里与 snippet 内容最相似的块」后，2 条均映射到块 16（sim=0.86），检索 top1 即块 16 → **坏例消除**。只读预验证确认：仅这 2 条发生映射变化，其余 52 条不变，无虚高；引用准确率同步升至 98.15%（LLM 引用修正后的块 16）。
- 「退货政策是什么」：目标块仍排 top5，映射修正无效——泛查询 + 同章节多块竞争，属真实检索排名问题，指向 §9.1.3 查询改写。
- 结论：坏例 3 → 1，指标 94.44% → 98.15%（双口径记录见 §6.5）。剩余唯一坏例为泛查询类，检索层优化（查询改写/放大 TOP_K）尚未实施。

**评测集扩容后坏例溯源（2026-08-29，255 块，126 条）**：
- 指标从 98.15% 回落到 **87.20%（召回）/ 85.60%（引用）**，坏例 **18**（recall_miss ×16 + citation_error ×2），dropped 1。回落符合预期（样本 54→126、文档 52→255、加入难例/边界例），即暴露真实短板，不掩盖。
- **recall_miss ×16 归类**：
  1. **泛查询（旧坏例延续）**：「请问你们平台的退货政策是什么？」目标块为章节首块（chunk 1），检索返回 2/5/131——答案分散在退货章节多个块，同章节竞争，仍指向 §9.1.3 查询改写。
  2. **跨章节/复合多条件问题**：会员权益（黑卡/金卡）、「优惠券+积分+退款」「满减+优惠券部分退款」——答案跨多个标题、单块无法覆盖全部知识点，需查询改写拆分子问题或跨块聚合。
  3. **新文档长尾主题**：跨境赔付时限、企业账期结算——知识库扩容后新增主题，目标块被同主题其它块稀释（如跨境丢失目标块 234，返回 229/233/228）。
  4. **精确条款句未进 top3**：「退款原路退回」「退款到账 1-3 工作日」——目标条款块与语义相近的相邻块竞争。
- **citation_error ×2**：签收后第 8 天退货（超 7 天）——模型引用了「7 天无理由」块而非「超期处理/按质量问题判定」块，引用指错，指向生成层引用后校验。
- **dropped 1**：一条新生成问题的 required_snippet 横跨两块边界，无单个 chunk 完整包含 → 该条剔除（如实记录），不做指标美化。
- 结论：扩容真实暴露系统短板 = 泛查询/跨章节复合问题检索 + 边界引用，作为下一步「坏例驱动优化」（§9.1.3 查询改写 / 引用后校验）的输入。
- 18 条坏例完整明细（question/expected_answer/required_snippet/期望块/实际命中或引用块/答案截断）存档：`03_测评集/reports/badcases_20260829_050057.md`。

**查询改写 + 引用后校验后坏例溯源（2026-08-29，双口径全量 255 块 · 126 条）**：
- **只读预验证（18 条坏例）**：改写仅修好 5/18 召回（4 条召回+引用全对、1 条仅修好召回）；引用后校验对 2 条 citation_error 无效（模型引用的块本身就是与句子最相似的块，只是 ≠ 期望块）。阈值标定：逐句相似度最低 0.62（多数 >0.7），`CITATION_CHECK_THRESHOLD=0.5` 保留有效引用、无误删，无需调整。
- **根因诊断（仍坏案例逐条打分）**：期望块在多路检索中普遍排第 4~6 名、被同章节高相关块挤掉（「黑卡权益」exp=15、「退款原路」exp=19 等 8/13 条期望块在任何子查询 top3 里都不出现）。**查询改写本质是多路召回，解决不了 bi-encoder 余弦排序能力不足**——期望块相关性分本身低于竞争块，换多少子查询都翻不进 top3。属「章节内块竞争」，**Rerank（交叉编码器）才是针对性解法**（§9.1.4；模型未缓存、离线装不上，列为二期）。
- **全量双口径（deepseek-chat，`_run_metrics.py`）**：关 87.20%/85.60%/坏例18 → 开（改写+后校验）**88.00%/88.00%/坏例15**。收益：召回 +1、引用 +3、坏例 -3（2 条 citation_error 全部消除；15 条 recall_miss 减 1）。
- **剩余 15 条 recall_miss**：仍为泛查询/跨章节复合问题（黑卡/金卡权益、退款原路/到账时限、账期结算、跨境丢失、价保、审核不通过、全额退款、上门取件、积分扣除等），期望块均排 top3 外——**真实排序短板，改写救不了**。完整报告：`02_优化/compare/compare_rewrite_20260829_065149.md`（md + json）。
- 结论（如实记录）：改写+引用后校验收益有限（坏例 18→15），核心短板仍在 bi-encoder 检索排序，二期优先启用 Rerank（§9.1.4）+ 放大候选池。

**Rerank 落地后坏例溯源（2026-08-29，全量双口径 255 块 · 126 条，开=改写+Rerank+引用后校验）**：
- **模型落地**：`BAAI/bge-reranker-v2-m3`（~2.2GB）已下载缓存；`core/rerank.py` 用 transformers 直调交叉编码器（FlagEmbedding 的 `prepare_for_model` 在 transformers≥5 已移除会崩溃并被 except 吞掉导致 rerank 静默失效——这是"rerank 前后数据没变化"的真正根因）。
- **全量双口径（deepseek-chat，`_run_metrics.py`）**：关 87.20%/85.60%/坏例18 → 开（改写+Rerank+校验）**92.00%/90.40%/坏例12**。相对上一轮开（改写+校验，无 Rerank）：召回 +5、引用 +3、坏例 -3（recall_miss 15→10）。
- **Rerank 修好 9 条 recall_miss**（黑卡/金卡权益、会员降级、退款到账时限、草莓、全额退款、价保、跨境丢失、预售价保），同时新回归 4 条 recall_miss（拆封 7 天无理由、签收质量问题流程、大促价保、黑卡三倍积分叠加——期望块仍未进 top3）。
- **新增 citation_error ×2**（发票抬头 exp=34、仓库验收 exp=2）：根因诊断（`_diag_rerank_badcases.py`）显示**答案内容正确**，期望块已进 top3（召回已修复），但模型引用了更直接相关的块而非评估期望块（如「仓库验收」引 chunk106"售后处理时效"而非 exp=chunk2"退换货政策"，后校验按语义相似度保留 chunk106）。属「引用指向合理但与评估期望块不一致」的边界 case，非错误答案。
- **排序缺口修复（2026-08-29）**：`retrieve/retrieve_multi` rerank 精排后曾按 bi-encoder `sort(key=c.score)` 重排、Rerank 顺序被丢弃（诊断实证 70/15/71 的 rerank 分 5.28/3.74/4.05 非降序）。已改为 `use_rerank=True` 时直出精排顺序。修复后开口径 citation_error ×3→×2、引用 89.60%→90.40%、坏例 13→12（「部分退款优惠券」被修复）。
- 完整报告：`02_优化/compare/compare_rewrite_20260829_110735.md`（md + json，含上一轮无 Rerank 存档对照）；排序修复前：`compare_rewrite_20260829_103228.md`。

---

## 9. 未来优化方向（暂不实施 · 经评估确认）

> 原则：一切优化围绕三项指标（召回率/引用准确率/坏例数）展开，**一次只改一个变量，永远用同一份评估集做回归对比**。凡改切块/embedding 的需重建知识库 + 新建版本快照，并评审评估基准一致性。

### 9.0 优化决策原则（先看坏例分布，别瞎优化）
| 主要坏例类型 | 说明 | 该先改哪一层 |
|---|---|---|
| `recall_miss` 占比高 | 没检索到正确资料 | **检索层**（优先级最高，收益最大） |
| `citation_error` 占比高 | 引用了但引用错/乱 | 生成层 + 引用后校验 |
| `hallucination` 占比高 | 无依据自圆其说 | 阈值闸门 + 引用校验 + prompt |
| `empty_answer` 占比高 | 该答却没答 | 阈值标定 + 兜底调优 |

> 经验：**先用 20% 精力修检索层（recall），通常换来 80% 改善**。当前 3 条坏例全为 `recall_miss` → 检索层优先。

### 9.1 检索层优化

**9.1.1 切块粒度优化（高优先 · 治本）**
- 现状：`会员体系` 等 235 字混合大块稀释查询语义，是 2 条坏例根因。
- 方向：`CHUNK_SIZE` 500→200 左右，按子知识点断开，块内保留父标题归属。
- 风险：切细后单块覆盖面变窄，需配合放大候选池 + 阈值重标定 + 指标验证，防止 Top3 召回不全。

**9.1.2 混合检索（BM25 + 稠密向量 + RRF）（中优先）**
- 价值：专有名词/SKU/政策编号等长尾实体靠关键词精确命中，弥补稠密检索对实体弱的短板。
- 局限：对"同章节多块竞争"帮助有限。

**9.1.3 查询改写 / 意图识别（✅ 已实施 2026-08-29 · 收益有限，见 §8.4）**
- 价值：把"一个面"的泛查询（如「退货政策是什么」）拆成多个具体子查询，多路召回合并，对泛查询类坏例对症。
- 已实现：`core/query_rewrite.py`（`deepseek-chat`）+ `retrieve_multi`；双口径验证见 §6.5/§8.4。
- 实测结论（126 条）：召回 87.20%→88.00%（+1），**仅对部分泛查询有效**；仍坏案例的期望块在任何子查询 top3 都不出现，属 bi-encoder 排序短板，改写救不了。
- 代价：每次多 1 次 LLM 调用 + 多路检索，延迟与成本上升。

**9.1.4 放大候选池 + 启用 Rerank（✅ 已实施 2026-08-29 · 全量验证收益显著）**
- 历史结论：切块优化前（43 块）与切块优化后（52 块）两轮验证，开/关 rerank 均无差异（坏例全为召回层 miss），当时未启用。
- **触发条件达成并落地（2026-08-29）**：语料扩至 **255 块**后出现大量"高度相关但答案不同"的同章节竞争块（§8.4：期望块普遍排第 4~6 名、被同类块挤掉）。已下载 `BAAI/bge-reranker-v2-m3`（~2.2GB，HF 镜像 + `HF_HUB_DISABLE_XET=1` 强制 LFS 规避 401），`core/rerank.py` 用 transformers 直调交叉编码器。
- 实施：候选放大 `RERANK_CANDIDATES=20` 召回 → rerank 精排取 top_k → 双口径对比。
- **实测（126 条，`_run_metrics.py`）**：开（改写+Rerank+校验）召回 87.20%→**92.00%**、引用 85.60%→**90.40%**、坏例 18→**12**；相对无 Rerank 开口径再 +5 召回、+3 引用、-3 坏例。**Rerank 确认为 bi-encoder 排序短板的针对性解法。**
- **排序缺口已修复（2026-08-29）**：`retrieve/retrieve_multi` rerank 精排后曾按 bi-encoder `sort(key=c.score)` 重排、Rerank 顺序被丢弃。已改为 `use_rerank=True` 时直出精排顺序（候选放大 20 → rerank 取 top_k → 直接返回，不再重排）。修复后引用 89.60%→90.40%、坏例 13→12。

**9.1.5 阈值与 TOP_K 自动寻优**
- 用评估集网格搜索 `MIN_SCORE`、`TOP_K` 最佳组合，把 0.35 变成贴合数据的最优值；切块或文档构成变化后需重标定。

**9.1.6 父子分块 / 语义分块**
- 小"子块"检索、命中后回带整段"父块"给模型，或换语义分块器：检索更精准、上下文更完整。

**9.1.7 换/微调 Embedding**
- 用领域数据微调 embedding 或换更强模型：底层相似度更准（成本高）。

**9.1.8 元数据过滤（语料大时）**
- 检索前按文档/业务线先过滤，缩小候选池、降低跨主题干扰。

**9.1.9 分层索引（十万级语料时）**
- 上层章节摘要粗筛 → 定位章节 → 细块精取。

### 9.2 生成层优化（治 citation_error / hallucination）
| # | 手段 | 做什么 |
|---|---|---|
| 2.1 | Prompt 强化 | 强制"每句结论必须带引用编号""资料不足就明说"，给 few-shot 示例 |
| 2.2 | 引用后校验(自洽核查) | ✅ 已实施 2026-08-29（`qa._post_check_citations`，bge-m3 语义校验：无支撑引用移除 / 非最优块改写为最相似块编号，阈值 0.5）：引用准确率 85.60%→88.00%，2 条 citation_error 全消除（详见 §8.4） |
| 2.3 | 兜底与置信分级 | 低于阈值/引用校验不过 → 回"资料不足"或标记转人工，而非硬编 |
| 2.4 | 温度与采样调参 | 客服场景追求稳定，temperature 调低（0–0.3）、关闭随机采样 |

### 9.3 评测体系升级
| # | 手段 | 做什么 |
|---|---|---|
| 3.1 | 评估集做难 | 加难例、边界例、易混淆例、多轮追问；定期混入线上真实难问（脱敏） |
| 3.2 | LLM-as-judge 自动判分 | 用 DeepSeek 对答案自动判"是否忠实原文/引用是否成立"，减少人工打分 |
| 3.3 | 坏例闭环 + 回归测试 | 每次改动后自动重跑整个评估集防止指标退化；坏例修复后全量重跑确认无新问题 |
| 3.4 | 改动对比自动报告 | 一键产出"改前 vs 改后"三大指标对比图，沉淀进 `02_优化/compare/` |
| 3.5 | 评估集质量优化 | 当前去重仅按 question 精确文本，措辞近似问题会残留（如两条「会员折扣与优惠券叠加」），需语义级去重 |

### 9.4 产品化与落地
| 模块 | 二期动作 |
|---|---|
| 知识更新 | 新增文档→自动增量入库/重 embedding；文档版本管理 |
| 反馈闭环 | 聊天页加"答案有用/没用"，没用的自动进坏例池→回流评估集 |
| 多租户与权限 | 上传、登录、数据隔离、审计日志（若上 SaaS） |
| 前端与部署 | Streamlit 换/并行正式前端(React)；Docker / Serverless 部署 |
| 成本与性能 | 问答缓存、异步处理、embedding 批量、必要量化 |

### 9.5 建议的二期上线顺序
1. **9.1.4 Rerank 已于 2026-08-29 实施并全量验证**（开=改写+Rerank+校验：召回 87.20%→92.00%、引用 85.60%→90.40%、坏例 18→12，排序缺口已修复）；**下一优先：9.1.5 阈值/TOP_K 寻优**；
2. **再上 9.3.2 LLM-judge**（自动判分）；
3. **然后 9.1.1 切块粒度 + 9.1.2 混合检索**（解决难例，冲高分）；
4. **最后 9.4.x 落地项**（反馈闭环→知识更新→前端/部署），把系统变成"可长期维护、别人能用"的产品。

> 每完成一步，在 `02_优化/optimize_log.md` 记一行：改了啥、指标从多少到多少。这是"持续迭代"的最好证据。

---

## 10. 作品集沉淀（`01_mvp/README.md` 要写这些）

- 项目一句话定位 + 架构图（文档→转码→入库→检索→生成→引用）。
- 三大指标基线 + 坏例分类截图（取自 `03_测评集/reports/`）。
- 二次优化前后对比图（取自 `02_优化/compare/`）+ 优化日志。
- 多格式文档源接入演示（docx/pdf/xlsx/pptx 转码 → 问答命中）。
- 演示录屏链接（3 分钟；说明见 `05_运行与交付/演示录屏说明.md`）。
- 二期路线图（接入更多数据源/多租户/正式前端）。
